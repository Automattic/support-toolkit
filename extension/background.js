// Background service worker
// No active message handlers - reserved for future use

chrome.runtime.onMessage.addListener((request, sender, sendResponse) => {
    // No handlers currently active
    return false;
});
